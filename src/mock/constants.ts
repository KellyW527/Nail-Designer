import type { StageSize } from "@/types";

export const DEFAULT_STAGE_SIZE: StageSize = {
  width: 1024,
  height: 768,
};

export const DEFAULT_HAND_BOUNDS = {
  x: 220,
  y: 110,
  width: 340,
  height: 500,
};
